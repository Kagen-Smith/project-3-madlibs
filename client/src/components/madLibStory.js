const MadLibStory = ({ story }) => {
    return Your;
    Mad;
    Lib;
    Story < /h2>
        < p > { story } < /p> {/ * Display;
    the;
    story;
    passed;
    prop * /;
};
/div>;
;
;
export default MadLibStory;
