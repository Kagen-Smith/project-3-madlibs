const TemplateCard = ({ template, onSelect }) => {
    return onClick = {}();
};
onSelect(template.id);
 > { /* Trigger callback on click */}
    < h3 > { template, : .title } < /h3> {/ * Template;
title * /;
({ template, : .description } < /p> {/ * Template);
description * /;
export {};
