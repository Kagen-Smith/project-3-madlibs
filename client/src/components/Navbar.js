const Navbar = () => {
    return ({ /* Navigation links to different routes */}
        < li > to) = "/" > Home < /Link></li >
        to;
    "/profile" > Profile < /Link></li >
        to;
    "/login" > Login < /Link></li >
        to;
    "/signup" > Sign;
    Up < /Link></li >
        /ul>
        < /nav>;
};
;
;
export default Navbar;
