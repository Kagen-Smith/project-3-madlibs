const Profile = ({ stories }) => {
    return Your;
    Saved;
    Stories < /h2>;
    { /* Display each saved story or show a message if no stories exist */ }
    {
        stories.length > 0 ? (stories.map((story, index) => key, { index } > { story } < /p>)) : No;
        saved;
        stories;
        yet < /p>;
    }
};
/div>;
;
;
export default Profile;
